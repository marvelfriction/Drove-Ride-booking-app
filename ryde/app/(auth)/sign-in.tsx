import { View, Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const SignIn = () => {
  return (
    <SafeAreaView>
      <Text className="bg-red-400">Sign-In</Text>
    </SafeAreaView>
  );
};
export default SignIn;
