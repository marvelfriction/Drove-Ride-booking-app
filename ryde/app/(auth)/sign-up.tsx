import { View, Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const SignUp = () => {
  return (
    <SafeAreaView>
      <Text className="bg-red-400">Sign-Up</Text>
    </SafeAreaView>
  );
};
export default SignUp;
