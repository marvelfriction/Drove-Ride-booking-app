import { View, Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Rides = () => {
  return (
    <SafeAreaView>
      <Text className="bg-red-400">Rides</Text>
    </SafeAreaView>
  );
};
export default Rides;
