import { View, Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const Profile = () => {
  return (
    <SafeAreaView>
      <Text className="bg-red-400">Profile</Text>
    </SafeAreaView>
  );
};
export default Profile;
